/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DateOnlyDirectiveDirective } from './DateOnlyDirective.directive';

describe('Directive: DateOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new DateOnlyDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
